# Codex 실행지시서 — B7-1 v2.0 / 첨부 계약·최소 미션 우선

이 패키지는 **명세 MD 5개와 실행지시서 1개**로 구성됩니다. 앱 코드, 실제 API 키, DB, 실행 완료 증거는 포함하지 않습니다. Codex가 아래 지시에 따라 구현합니다. 이전 v1 문서와 섞어 사용하지 않습니다.

## 1. 사용자 준비

개인 풀스택 구현용 폴더에 압축 내용을 다음 구조로 넣습니다. Organization의 실제 저장소를 덮어쓰지 않습니다. 이전 개인 폴더에 소스가 생겼다면 백업과 현재 diff를 먼저 확인하고, 같은 이름의 입력 명세 6개만 교체합니다. 실제 .env, DB, 소스는 삭제하지 않습니다.

```text
개인 구현 폴더/
├── AGENTS.md
├── CODEX_START.md
└── docs/
    ├── MISSION_REQUIREMENTS.md
    ├── PROJECT_PLAN.md
    ├── IMPLEMENTATION_SPEC.md
    └── MILESTONES.md
```

VS Code에서 ‘폴더 열기’로 이 루트를 열고 Codex 확장에서 같은 로컬 프로젝트를 선택합니다. Codex 앱이나 CLI를 사용해도 이 폴더가 작업 폴더여야 합니다. ZIP만 채팅에 첨부하거나 docs 하위 폴더만 열지 않습니다. 일반 MD는 자동 지침이 아니므로 다음 프롬프트로 직접 읽게 합니다. OpenAI 안내 출처는 PROJECT_PLAN의 E01/E02입니다.

본인의 Git user.name과 user.email이 정확한지 확인합니다. 확인되지 않은 주소를 예시로 넣어 커밋하지 않습니다. 팀 공용 계정이나 다른 팀원의 정보라면 현재 저장소의 로컬 설정만 바꾸거나 커밋을 대기합니다. 전역 설정은 건드리지 않습니다.

## 2. 그대로 붙여넣는 시작 프롬프트

```text
현재 작업 폴더의 CODEX_START.md를 읽고 실행해 주세요.

이 폴더는 팀 Organization과 별개인 개인 풀스택 기준 구현용입니다.
루트 AGENTS.md와 docs의 네 문서를 분할해서라도 끝까지 읽고,
수록된 최신 첨부 원문 S01~S11 및 공식 미션을 이전 명세보다 우선해 주세요.

먼저 G0 준비와 M01~M21 추적표, 현재 Git author/브랜치 상태를 확인하고,
MILESTONES.md의 R01부터 실제 구현·실행·테스트를 순서대로 진행해 주세요.
전체를 한 번에 만든 뒤 커밋을 소급 분할하지 마세요.

정확한 본인 Git 이름·이메일이 확인된 경우, feat/fullstack-lee에서
실제 기능 단위 검증 후 본인 명의의 로컬 커밋을 생성하는 것을 승인합니다.
20개 계획을 따라 최소10개 이상의 의미 있는 실제 작업 이력을 남기되,
빈 커밋·타인 명의·가짜 기여·이전 완료 코드의 인위적 분할은 금지합니다.
작성자 정보가 없거나 다른 사람 값이면 커밋만 대기하고 필요한 설정을 보고하세요.

최신 계약 username/password, question → answer/latency_ms,
chat_logs.response, detail 문자열, history 배열, 중복가입400을 유지하세요.
첨부끼리 충돌하는 부분은 PROJECT_PLAN의 C/D 결정과 테스트로 처리하세요.

먼저 인증·문맥·DB·로그·실패처리까지 갖춘 미션 최소 기능을 완성하고
LOCAL_MINIMUM 결과를 보고한 뒤 실제 AI/배포 준비와 증빙을 마무리하세요.
프론트 추출·팀 백엔드 재접합·RAG·추가기능은 하지 마세요.

원격 push/PR 생성·승인·merge, AWS 생성·설정변경, 실제 유료 AI 호출,
외부 공개는 별도 승인 없이 하지 마세요. PR 초안과 다음 명령은 작성하세요.
실제 AI·외부 URL·팀4명 각10커밋은 로컬 성공과 분리해 상태를 기록하세요.
계획만 출력하고 끝내지 말고 가능한 로컬 구현과 검증까지 진행하세요.
```

## 3. Codex가 먼저 할 일

현재 폴더와 Git 상태를 확인한다. 빈 저장소라는 전제와 달리 이미 앱이 있으면 사용자 변경과 데이터를 보존한다. 루트 AGENTS → MISSION → PROJECT_PLAN → IMPLEMENTATION_SPEC → MILESTONES → 본 지시서를 실제로 읽는다. 내용이 길다는 이유로 원자료를 건너뛰지 않는다.

이름, 이메일, 브랜치, 원격 주소, 현재 SHA와 작업 범위를 확인한다. 저장소 초기화는 개인 작업 폴더에서만 하고 기존 Git 이력과 원격을 재설정하지 않는다. 필요하면 첫 기준 커밋 뒤 로컬 main/develop 참조를 만들되, 최초 초기화와 PR merge를 혼동하지 않는다. 다른 세 팀원의 브랜치나 명의는 만들지 않는다.

G0에서 ignore, 설정, API·스키마, 협업 양식, README, 미션 상태표, 커밋 계획을 먼저 구성한다. 첨부파일은 원문 블록에서 복원할 수 있다. AGENTS 원문을 다시 추출해 현재 지침을 덮어쓰거나, project_plan의 대소문자 중복 파일을 만들지 않는다.

초기 보고에는 읽은 문서, 실제 폴더와 Git 상태, 외부 정보·권한의 미확인 사항, 적용할 C 항목, 다음 R 번호를 짧게 적는다. 그 뒤 같은 작업에서 개발을 계속한다. 비밀값은 출력하지 않는다.

## 4. 구현과 실행

R01~R20은 한 기능 구현 → 관련 테스트 → diff·비밀값 검사 → 정확한 파일 stage → 본인 commit 순서로 진행한다. 로컬 설치, 테스트, 서버 실행은 허용된 작업 공간과 가상환경 안에서 한다. 실행 환경의 명령 승인 정책을 따르며 전역 설치나 보안 설정 해제로 우회하지 않는다.

첨부 requirements의 라이브러리 선택을 유지하고 설치, pip check, C06 해시 smoke test를 먼저 확인한다. 패키지 네트워크나 브라우저 도구가 없으면 정확한 오류와 재실행 명령을 남긴다. 시험하지 않은 해시 조합이나 앱 기능을 PASS로 꾸미지 않는다.

개발용 초기화 명령은 구현한 스크립트가 실제로 존재한 뒤 사용한다. 기본 흐름은 다음과 같다.

```text
python -m venv .venv
가상환경의 python으로 requirements/검증된 constraints 설치
python scripts/init_env.py --mode mock
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
python -m pytest
```

위는 작업 순서이며 아직 없는 스크립트를 먼저 실행하라는 뜻이 아니다. Codex는 README에 macOS/Linux와 Windows에서 실제로 사용할 명령을 각각 작성한다. PowerShell 정책을 불필요하게 바꾸지 않고 가상환경의 python 실행파일을 직접 호출하는 방법도 안내한다. 실제 사용한 명령과 성공 여부를 기록한다.

로컬 서버가 켜지면 브라우저 `http://127.0.0.1:8000`에서 가입, 로그인, 질문, 기록을 확인한다. HTML 파일을 직접 여는 파일 경로는 쓰지 않는다. 브라우저 도구가 없다면 수동 검증 순서를 작성하고 해당 검사는 NOT_RUN으로 둔다.

## 5. 사람당10커밋을 실제로 다루는 방법

현재 Codex 실행은 본인만의 커밋을 만든다. 다른 팀원 세 명의 실제 작업까지 생성할 수는 없으며 역할표를 완료표처럼 채우지 않는다. R06~R15의 프론트 10개 작업은 검토 가능한 단위로 분리하되 이번에는 추출하거나 이식하지 않는다.

본인의 개인 저장소에서 20개를 만들었다고 최종 팀 과제의 각 10개 조건이 충족되는 것은 아니다. 이후 평가 대상 저장소의 실제 SHA, PR, 최종 브랜치를 대조해야 한다. 현재는 TEAM_HISTORY=NEEDS_TEAM_REVIEW로 남기고 원격 검증이 승인되면 확인한다. 일괄 복사한 1개 커밋과 개별 이력 보존이 같다고 가정하지 않는다.

Create a merge commit을 보수적인 운영안으로 기록하되 설정을 임의로 변경하지 않는다. squash가 강제되어 있다면 평가 기준과 팀 운영 방식을 확인할 항목으로 남긴다. PR 10개가 필수라는 새 조건을 만들지 않는다. 커밋의 내용, 작성자, 검증, 최종 반영 여부가 중요하다.

## 6. 실제AI와 배포에 필요한 승인

실제 키가 있어도 무제한 호출 권한으로 해석하지 않는다. 실제 코디세이 URL, 모델, 사용 권한과 허용 호출 범위가 확인되면 최소 두 턴 검증을 별도 승인 아래 수행한다. 공급자 환경이 없으면 adapter의 가짜 transport와 Mock까지 검증하고 REAL_AI=BLOCKED_EXTERNAL로 남긴다.

EC2 계정, 비용, 리전, 호스트, 보안 그룹, 경로, HTTPS 예외가 확인되기 전에는 배포 문서와 템플릿만 작성한다. ‘프리티어이니까 무료’라며 리소스를 만들지 않는다. 2GB Swap 규칙은 포함하되 OS 변경은 승인 후 수행한다. PUBLIC_URL이 없으면 미션 미완료 항목임을 명시한다.

실제 외부 접속과 팀 PR에는 별도 권한이 필요하다. 이를 대신해 개인 로컬에서 가짜 성공 결과를 만들지 않는다. 그 밖의 로컬 작업은 완성한다.

## 7. 중간/최종 보고 형식

LOCAL_MINIMUM을 완성했을 때 먼저 다음 내용을 보고하고 증빙 파일에 남긴다. 최종에는 같은 표를 갱신한다.

```text
현재 branch / 마지막 SHA / 실제 본인 커밋 수:
LOCAL_MINIMUM: PASS / FAIL / NOT_RUN
REAL_AI: PASS / FAIL / BLOCKED_EXTERNAL / NOT_RUN
PUBLIC_URL: PASS / FAIL / BLOCKED_EXTERNAL / NOT_RUN
TEAM_HISTORY: PASS / FAIL / NEEDS_TEAM_REVIEW / NOT_RUN

이번에 실제 구현한 기능과 파일:
실제 실행 명령·패키지 버전·테스트 수·실패:
브라우저 테스트 도구/수동 여부:
M01~M21 중 미충족 항목:
C06/C10/C11 등 팀 확인이 남은 결정:
현재 본인 commit SHA/의미/검증 및 PR 초안:
외부 키·권한·작성자 확인 등 필요한 입력:
다음 실행할 단계/명령:
```

`docs/IMPLEMENTATION_STATUS.md`와 `docs/CONTRIBUTIONS.md`는 실제 결과로만 작성한다. 계획의 체크리스트를 자동으로 PASS 처리하지 않는다. 최소 미션이 끝나지 않았는데 모양 꾸미기나 새 프레임워크를 추가하지 않는다.

## 8. 중단 후 재개 프롬프트

```text
CODEX_START.md와 현재 AGENTS.md, docs/IMPLEMENTATION_STATUS.md,
docs/CONTRIBUTIONS.md, 실제 git status/log를 다시 읽고 재개해 주세요.
최신 S01~S11 계약은 유지합니다. 완료된 R단계를 반복 구현하지 말고
미완료 단계부터 검증·본인 로컬 커밋을 이어가세요.
.env/DB/기존 작업은 보존하고, 아직 승인되지 않은 원격·AI·AWS 작업은 하지 마세요.
```

상태 문서가 없다면 Git과 소스를 확인하여 현재 상태부터 사실대로 복구한다. 존재하지 않는 과거 커밋이나 실행 결과를 추정해서 쓰지 않는다.
